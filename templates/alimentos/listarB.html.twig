{% extends 'alimentos/layout.html.twig' %}

  {% block contenido %}

  <table>
      <tr>
          <th>alimento (por 100g)</th>
          <th>energía (Kcal)</th>
          <th>grasa (g)</th>
          <th>wiki</th>
          <th>XML</th>
          <th>PDF</th>
          <th>Editar</th>
          <th>Borrar</th>
      </tr>
      {% for alimento in alimentos %}
      <tr>
          <td><a href="{{ path('app_alimentos_ver', {'id': alimento.id}) }}">{{alimento.nombre}}</a></td>
          <td>{{ alimento.energia }}</td>
          <td>{{ alimento.grasatotal }}</td>
		  <td><a href="http://es.wikipedia.org/wiki/{{ alimento.nombre }}" class="cajamodal">wiki</a></td>
		  <td><a href="{{ path('app_alimentos_ver', {'id': alimento.id}) }}&ext=xml" target="_blank">Ver en XML</a></td>
		  <td><a href="{{ path('app_alimentos_ver', {'id': alimento.id}) }}&ext=pdf" target="_blank">Ver en pdf</a></td>

      </tr>
      {% endfor %}
	  

  </table>


  {% endblock %}