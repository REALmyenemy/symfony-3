{% extends 'alimentos/layout.html.twig' %}

  {% block contenido %}

 <form name="formBusqueda" action="{{ path('app_alimentos_buscarEnergia') }}" method="POST">

      <table>
          <tr>
              <td>Energ&iacute;a alimento:</td>
              <td><input type="text" name="energia" value="{{ energia }}"></td>

              <td><input type="submit" value="buscar"></td>
          </tr>
      </table>

      </table>

  </form>

  {% if resultado %}
  {% include 'alimentos/_tablaAlimentos.html.twig' with {'alimentos': resultado} %}
  {% endif %}

 {% endblock %}